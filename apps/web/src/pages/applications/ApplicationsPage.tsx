import { PagePlaceholder } from '../../shared/components/PagePlaceholder'

export const ApplicationsPage = () => {
  return (
    <PagePlaceholder
      title="Applications"
      description="Application inventory and environment state will live here."
    />
  )
}
