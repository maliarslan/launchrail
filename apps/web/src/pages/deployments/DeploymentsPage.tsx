import { PagePlaceholder } from '../../shared/components/PagePlaceholder'

export const DeploymentsPage = () => {
  return (
    <PagePlaceholder
      title="Deployments"
      description="Deployment history, filtering, and lifecycle actions will live here."
    />
  )
}
