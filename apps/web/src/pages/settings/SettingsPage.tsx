import { PagePlaceholder } from '../../shared/components/PagePlaceholder'

export const SettingsPage = () => {
  return (
    <PagePlaceholder
      title="Settings"
      description="Organization, user, and environment administration will live here."
    />
  )
}
